import React from 'react';

export interface Role {
  icon: React.ReactNode;
  title: string;
  description: string;
}

export interface Benefit {
  icon: React.ReactNode;
  title: string;
  description: string;
}
